[project]
name = "openai-agent"
version = "0.1.0"
description = "Autonomous Python coding agent with iterative tool calling, project inspection, editing, and execution."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
]
